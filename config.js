const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "maxox2004",
    password: "adminadmin",
    database: "dentaldb",
  },
};
module.exports = config;
